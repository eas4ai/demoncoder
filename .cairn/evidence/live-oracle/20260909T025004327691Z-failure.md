# Live Oracle prerequisite failure

Observed from the configured Claude subscription Oracle (model fable, effort high).
The existing live_oracle.py --run command executed the ignored live_verdict_pair
production test. It failed before recording a verdict pair:

    Claude turn failed; check subscription login and model availability

A read-only `claude auth status --json` check reported loggedIn=true,
authMethod=claude.ai, apiProvider=firstParty and subscriptionType=max.
A bounded no-tools, no-session-persistence diagnostic using the same fable/high
selection returned exit 1 and is_error=true:

    You've reached your Fable limit. Switch to another model, or manage usage
    credits at claude.ai/settings/usage?from=cc_cli_limit_message, to continue.

No proposed Oracle tool was executed. No account, billing method or saved model
was changed. This records an external availability limitation, not a passing
Oracle check. Evidence source: executed tool outputs from this follow-up session.
