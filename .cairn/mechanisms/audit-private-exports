command: cargo test --locked --test workflow_workspace --test subagent_worktrees --test developer_access && cargo install --path . --locked --root "$HOME/.cargo" && DEMONCODER_TEST_BINARY="$HOME/.cargo/bin/demoncoder" python3 tests/audit_remediation.py --case privacy
inputs:
  - Cargo.toml
  - Cargo.lock
  - src/
  - tests/
  - README.md
  - docs/spec/
requirements:
  - AUD-001
  - AUD-002
